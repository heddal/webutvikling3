import React from 'react';
import { makeStyles } from '@material-ui/styles';

const Infotext = (props) => {

    const useStyles = makeStyles( theme => ({

    }));

const classes = useStyles();

return (
    <div>
        <h2> BYNAVN </h2>
        <p> Informajon om byen</p>
    </div>
)
}

export default Infotext